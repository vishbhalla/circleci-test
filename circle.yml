deployment:
  prod:
    branch: master
    commands:
      - aws s3 cp file1 s3://vishtest-kon-cloud/file1.$CIRCLE_BUILD_NUM.txt

test:
  post:
    - aws s3 ls s3://vishtest-kon-cloud/file1.$CIRCLE_BUILD_NUM.txt
